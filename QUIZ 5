#include <stdio.h>

int main() {
    float savings, total = 0, average, shortage;
    float goal = 500.00;

    printf("========================================\n");
    printf("        DAILY SAVINGS TRACKER\n");
    printf("========================================\n");

    // Loop for 7 days
    for (int day = 1; day <= 7; day++) {
        printf("Enter savings for Day %d: PHP ", day);
        scanf("%f", &savings);

        total += savings;
    }

    // Calculate average
    average = total / 7;

    printf("\n========================================\n");
    printf("          SAVINGS SUMMARY\n");
    printf("========================================\n");

    printf("Total Savings: PHP %.2f\n", total);
    printf("Daily Average: PHP %.2f\n", average);

    // Check if goal is reached
    if (total >= goal) {
        printf("Status: GOAL REACHED\n");
    } else {
        shortage = goal - total;
        printf("Status: GOAL NOT REACHED\n");
        printf("You need PHP %.2f more to reach your goal.\n", shortage);
    }

    return 0;
}
